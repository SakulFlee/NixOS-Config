{ ... }:
{
  services.aria2 = {
    enable = true;
    settings = {
      dir = "/home/sakulflee/Downloads/"
    };
    openPorts = true;
  };

  # mdns resolution
  system.nssDatabases.hosts = [ "files" "mdns_minimal [NOTFOUND=return]" "dns" "mdns" ];
}
